import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

export function InspectionsPage() {
  return (
    <PagePlaceholder
      title="Inspections"
      description="Phase 4 will add offline-capable inspections with findings and resolution tracking."
    />
  );
}
