import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

export function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Phase 2 will add task CRUD, kanban, and filters by branch/priority/status."
    />
  );
}
